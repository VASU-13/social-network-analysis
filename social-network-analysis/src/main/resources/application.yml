server:
  port: 8080

spring:
  application:
    name: social-network-analysis
  datasource:
    url: jdbc:mysql://localhost:3306/sna
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect